<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Разрешения должны быть ПЕРЕД тегом application -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- Разрешение для Android 10+ -->
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />

    <!-- Разрешение для интернета -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BusTicketApp2"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="31">

        <!-- Ваши активности -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Остальные активности -->
        <activity android:name=".RegistrationActivity" android:screenOrientation="portrait" />
        <activity android:name=".BookingActivity" android:screenOrientation="portrait" />
        <activity android:name=".TripsActivity" android:screenOrientation="portrait" />
        <activity android:name=".TripDetailsActivity" android:screenOrientation="portrait" />
        <activity android:name=".MapActivity" android:screenOrientation="portrait" />
        <activity android:name=".SalesActivity" android:screenOrientation="portrait" />
        <activity android:name=".ReportsActivity" android:screenOrientation="portrait" />
        <activity android:name=".UserManagementActivity" android:screenOrientation="portrait" />
        <activity android:name=".AboutActivity" android:screenOrientation="portrait" />
        <activity android:name=".ReceiptActivity" android:screenOrientation="portrait" />
        <activity android:name=".SimpleSeatSelectionActivity" android:screenOrientation="portrait" />
        <activity android:name=".MultiBookingActivity" android:screenOrientation="portrait" />
        <activity android:name=".MultiSeatSelectionActivity" android:screenOrientation="portrait" />
        <activity android:name=".RouteMapActivity" android:screenOrientation="portrait" />
        <activity android:name=".SeatSelectionActivity" android:screenOrientation="portrait" />

        <!-- Для Google Maps -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDBnpKh31IjY1cRxzTNZdzM76J-aNmyMMk" />

        <!-- Провайдер для FileProvider -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Сервис для очистки билетов -->
        <service
            android:name=".BookingCleanupService"
            android:exported="false"
            android:enabled="true" />

        <!-- Активность для сканера QR (если используете) -->
        <activity android:name=".QRScannerActivity" android:screenOrientation="portrait" />

    </application>

</manifest>